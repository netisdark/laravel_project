services:
  - type: web
    name: my-laravel-app
    env: php
    runtime: php-8.0   # Use the correct PHP version
    buildCommand: 'composer install'
    startCommand: 'php artisan serve --host=0.0.0.0 --port=80'
    plan: starter
